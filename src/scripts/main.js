bespoke.from('article', {
  keys: true,
  touch: true,
  bullets: 'li, .bullet',
  scale: true,
  hash: true,
  progress: true,
  state: true,
  forms: true
});

function cek_kebenaran(n) 
{
	if (n==1)
		alert('Benar');
	else
		alert('Salah');
}